---
import SessionTimerWrapper from '../../components/SessionTimerWrapper';
import Layout from '../../layouts/Layout.astro';

export const prerender = false;

const { id } = Astro.params;
---

<Layout title="Pilates Journal - Séance">
  <div class="container mx-auto px-4 py-8 max-w-7xl">
    <SessionTimerWrapper client:load sessionId={id || ''} />
  </div>
</Layout>
